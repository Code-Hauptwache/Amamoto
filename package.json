{
  "name": "amamoto",
  "version": "0.1.0",
  "description": "Traffic simulation platform with WebAssembly",
  "private": true,
  "scripts": {
    "build:wasm": "emcmake cmake -B build && cmake --build build",
    "serve": "http-server src/web/public -o -c-1",
    "build": "npm run build:wasm",
    "start": "npm run serve"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}